<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Payroll System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/hawala.css">
</head>
<body class="hawala-page">
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-2 sidebar">
                <h3>Navigation</h3>
                <a href="/bank">Bank Transfer</a>
                <a href="/hawala" class="active">Hawala Transfer</a>
                <a href="/bank-history">Bank History</a>
                <a href="/hawala-history">Hawala History</a>
                <a href="/add-market">Add Market</a>
                <a href="/add-bank">Add Bank</a>
                <a href="/add-account">Add Account</a>
            </div>

            <!-- Main Content -->
            <div class="col main-content">
                <h1>Hawala Transfer System</h1>
                
                <!-- Markets Section -->
                <div class="transfer-form mb-4">
                    <h3 class="mb-4">Select Market</h3>
                    <div class="row g-3" id="marketCardsContainer">
                        <% if (markets && markets.length > 0) { %>
                            <% markets.forEach(function(market) { %>
                                <div class="col-md-3">
                                    <div class="card market-card" data-market="<%= typeof market === 'object' ? market.name : market %>">
                                        <div class="card-body">
                                            <h5 class="card-title"><%= typeof market === 'object' ? market.name : market %></h5>
                                        </div>
                                    </div>
                                </div>
                            <% }); %>
                        <% } else { %>
                            <div class="col-12">
                                <p class="text-center text-muted">No markets available. Please <a href="/add-market">add a market</a> first.</p>
                            </div>
                        <% } %>
                    </div>
                </div>

                <div class="transfer-form">
                    <form id="hawalaForm" novalidate>
                        <div class="row g-4">
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="selectedMarket" name="market" readonly required placeholder=" ">
                                    <label for="selectedMarket">Selected Market</label>
                                </div>
                                <% if (!markets || markets.length === 0) { %>
                                    <small class="text-muted">No markets available. Please <a href="/add-market">add a market</a> first.</small>
                                <% } %>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <select class="form-select" id="accountSelect" name="accountName" required>
                                        <option value="">Select account</option>
                                        <% if (accounts && accounts.length > 0) { %>
                                            <% accounts.forEach(function(account) { %>
                                                <option value="<%= typeof account === 'object' ? account.name : account %>"><%= typeof account === 'object' ? account.name : account %></option>
                                            <% }); %>
                                        <% } %>
                                    </select>
                                    <label for="accountSelect">Account Name</label>
                                </div>
                                <% if (!accounts || accounts.length === 0) { %>
                                    <small class="text-muted">No accounts available. Please <a href="/add-account">add an account</a> first.</small>
                                <% } %>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <input type="text" class="form-control" id="nusingaInput" name="nusinga" placeholder=" " required>
                                    <label for="nusingaInput">Nusinga</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-floating">
                                    <select class="form-select" id="transactionTypeSelect" name="transactionType" required>
                                        <option value="">Select transaction type</option>
                                        <option value="send">Send Money</option>
                                        <option value="receive">Receive Money</option>
                                    </select>
                                    <label for="transactionTypeSelect">Transaction Type</label>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="d-flex gap-2 align-items-start">
                                    <div class="form-floating" style="flex: 0 0 120px; min-width: 100px;">
                                        <select class="form-select" id="currencySelect" name="currency" required>
                                            <option value="USD" selected>USD</option>
                                            <option value="EUR">EUR</option>
                                            <option value="IQD">IQD</option>
                                        </select>
                                        <label for="currencySelect">Currency</label>
                                    </div>
                                    <div class="form-floating flex-grow-1">
                                        <input type="number" class="form-control" id="amountInput" name="amount" placeholder="Amount" required>
                                        <label for="amountInput">Amount</label>
                                    </div>
                                </div>
                            </div>
                            <div class="col-12">
                                <div class="form-floating">
                                    <textarea class="form-control" id="purposeTextarea" name="purpose" rows="3" placeholder=" " style="min-height: 100px;" required></textarea>
                                    <label for="purposeTextarea">Transfer Description</label>
                                </div>
                            </div>
                            <div class="col-12 mt-4">
                                <button type="submit" class="btn btn-primary w-100 py-3">Process Hawala Transfer</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Success Modal -->
    <div class="modal fade" id="successModal" tabindex="-1" aria-labelledby="successModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="successModalLabel">Transaction Successful</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="text-center mb-4">
                        <i class="bi bi-check-circle text-success" style="font-size: 3rem;"></i>
                    </div>
                    <p class="text-center" id="successMessage"></p>
                    <div class="transaction-details mt-3">
                        <p><strong>Amount:</strong> <span id="modalAmount"></span></p>
                        <p><strong>Market:</strong> <span id="modalMarket"></span></p>
                        <p><strong>Account Name:</strong> <span id="modalAccountName"></span></p>
                        <p><strong>Nusinga:</strong> <span id="modalNusinga"></span></p>
                        <p><strong>Type:</strong> <span id="modalType"></span></p>
                        <p><strong>Purpose:</strong> <span id="modalPurpose"></span></p>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-primary" data-bs-dismiss="modal">Close</button>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/hawala.js"></script>
</body>
</html>