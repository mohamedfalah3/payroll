<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Market - Payroll System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css">
    <style>
        :root {
            --primary-color: #2563eb;
            --secondary-color: #1e40af;
            --background-color: #f1f5f9;
            --text-color: #1e293b;
        }

        body {
            background-color: var(--background-color);
            color: var(--text-color);
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
        }

        .sidebar {
            height: 100vh;
            background: linear-gradient(145deg, var(--primary-color), var(--secondary-color));
            padding-top: 2rem;
            position: fixed;
            left: 0;
            z-index: 1000;
            width: 250px;
        }

        .sidebar h3 {
            color: white;
            font-size: 1.25rem;
            padding: 0 1.5rem;
            margin-bottom: 2rem;
            font-weight: 500;
        }

        .sidebar a {
            color: rgba(255, 255, 255, 0.9);
            text-decoration: none;
            padding: 0.75rem 1.5rem;
            display: block;
            transition: all 0.3s ease;
            border-radius: 0 25px 25px 0;
            margin: 0.5rem 0;
        }

        .sidebar a:hover, .sidebar a.active {
            background-color: rgba(255, 255, 255, 0.1);
            color: white;
            transform: translateX(10px);
        }

        .main-content {
            margin-left: 250px;
            padding: 2rem;
        }

        .market-form {
            background: white;
            padding: 2rem;
            border-radius: 16px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            max-width: 600px;
            margin: 0 auto;
        }

        .markets-list {
            background: white;
            padding: 2rem;
            border-radius: 16px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
            margin-top: 2rem;
            max-width: 600px;
            margin: 2rem auto 0;
        }

        .form-label {
            font-weight: 500;
            color: var(--text-color);
            margin-bottom: 0.5rem;
        }

        .form-control {
            border: 1.5px solid #e2e8f0;
            padding: 0.75rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.1);
        }

        .btn-primary {
            background-color: var(--primary-color);
            border: none;
            padding: 0.75rem 1.5rem;
            font-weight: 500;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background-color: var(--secondary-color);
            transform: translateY(-2px);
        }

        .market-list-item {
            padding: 1rem;
            border-bottom: 1px solid #e2e8f0;
            display: flex;
            align-items: center;
        }

        .market-list-item:last-child {
            border-bottom: none;
        }

        .market-icon {
            color: var(--primary-color);
            margin-right: 1rem;
            font-size: 1.25rem;
        }

        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }
            .main-content {
                margin-left: 0;
                padding: 1rem;
            }
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <!-- Sidebar -->
            <div class="col-md-2 sidebar">
                <h3>Navigation</h3>
                <a href="/bank">Bank Management</a>
                <a href="/hawala">Hawala Management</a>
                <a href="/add-market" class="active">Add Market</a>
                <a href="/add-bank">Add Bank</a>
                <a href="/add-account">Add Account</a>
            </div>

            <!-- Main Content -->
            <div class="col main-content">
                <div class="market-form">
                    <h2 class="mb-4">Add New Market</h2>
                    <% if (error) { %>
                        <div class="alert alert-danger mb-3">
                            <%= error %>
                        </div>
                    <% } %>
                    <form action="/add-market" method="POST">
                        <div class="mb-3">
                            <label for="marketName" class="form-label">Market Name</label>
                            <input type="text" class="form-control" id="marketName" name="marketName" required>
                        </div>
                        <button type="submit" class="btn btn-primary w-100">Add Market</button>
                    </form>
                </div>

                <div class="markets-list">
                    <h3 class="mb-4">Current Markets</h3>
                    <% if (markets && markets.length > 0) { %>
                        <% markets.forEach(market => { %>
                            <div class="market-list-item">
                                <i class="bi bi-shop market-icon"></i>
                                <%= market %>
                            </div>
                        <% }); %>
                    <% } else { %>
                        <p class="text-center text-muted">No markets added yet</p>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>